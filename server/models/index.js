const User = require('./User');
const Genre = require('./Genre');
const Topic = require('./Topic');
const Thread = require('./Thread');
const Reply = require('./Reply');

module.exports = { User, Genre, Topic, Thread, Reply };